{
  "_doc": "Copy to superadmin-recipients.json (gitignored in public repos) or seed via SETUP_ADMIN_EMAIL / INSTANCE_ADMIN_EMAILS / first-boot admin. Fake placeholders only — never commit real emails. Grants /admin access only; nothing is sent anywhere.",
  "recipients": [
    { "email": "admin@example.com" },
    { "email": "ops@example.com" }
  ]
}
