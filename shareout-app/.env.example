# Copy to .env — never commit. Admin provides deploy token.
# Used by ship.sh, wrangler deploy, and npm run dev:prod (--remote needs auth).

cloudfare_token=

# Required for R2 skill sync / ship / staging bootstrap (no hardcoded fallback).
CLOUDFLARE_ACCOUNT_ID=
