/**
 * Backward-compatible re-exports for the comments data tier.
 * Implementation lives in `./comments/` — see `handler.ts` for the module map.
 */
export { handleComments, createCommentForTool } from './comments/handler';
