/**
 * ShareOut Design System - Design Tokens
 * Canonical tokens now live in the @shareout/design-tokens workspace package
 * (plan §12). This module re-exports them so existing imports keep working and
 * there is one source of truth in code.
 */
export * from '@shareout/design-tokens';
