/**
 * Home page styles — Browser loading
 * @module design-system/pages/home/browser-loading
 */

/** CSS rules for: Browser loading */
export const browserLoadingStyles = `/* ── Browser loading ────────────────────────────────── */
#artifactBrowser { min-height: 120px; }
.artifact-card.is-filtered-out { display: none !important; }

`;
