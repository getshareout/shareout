/**
 * Home page styles — Sidebar tags filter
 * @module design-system/pages/home/sidebar-tags-filter
 */

/** CSS rules for: Sidebar tags filter */
export const sidebarTagsFilterStyles = `/* ── Sidebar tags filter ────────────────────────────── */
.rail-tags-list { display: flex; flex-direction: column; gap: 2px; }
.tag-btn .nav-label { text-transform: none; }
.tag-btn.active { background: var(--color-primary-light); color: var(--color-primary); }

`;
