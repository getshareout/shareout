/**
 * Home page styles — Shell layout
 * @module design-system/pages/home/shell-layout
 */

/** CSS rules for: Shell layout */
export const shellLayoutStyles = `/* ── Shell layout ───────────────────────────────────── */
.shell {
  display: grid;
  grid-template-columns: 300px 1fr;
  align-items: start;
  width: 100%;
  min-height: 100vh;
}

`;
