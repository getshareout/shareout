/**
 * Home page styles — base
 * @module design-system/pages/home/base
 */

/** CSS rules for: base */
export const baseStyles = `
html, body { min-height: 100vh; }
/* Reserve the scrollbar gutter so the toolbar's right edge stays aligned with the
   fixed account pill whether or not the page is scrolled (no shift on load). */
html { scrollbar-gutter: stable; }

`;
